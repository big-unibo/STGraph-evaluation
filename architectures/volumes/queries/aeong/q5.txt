MATCH (environment:Infrastructure)-[:hasInfrastructure]-(device:Sensor)-[:hasTemperature]-(temp:Temperature) TT FROM 0 TO {TSTAMPTO_PARAM} RETURN device, environment, MAX(temp.temperature) AS maxValue;
